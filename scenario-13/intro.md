# Network Policy Selection

## Contexte

Deux deployments existent: Frontend (namespace `frontend`) et Backend (namespace `backend`). Vous devez analyser plusieurs NetworkPolicies et choisir la moins permissive.

## Objectifs

- Analyser des NetworkPolicies
- Comprendre les namespaceSelector et podSelector
- Appliquer le principe du moindre privilège
