# NodePort Service

## Contexte

Un deployment `nodeport-deployment` existe dans le namespace `relative`. Vous devez le configurer pour l'exposer via NodePort.

## Objectifs

- Configurer les ports dans un deployment
- Créer un Service NodePort avec un port spécifique
- Comprendre targetPort, port, et nodePort
